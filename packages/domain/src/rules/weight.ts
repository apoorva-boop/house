import type { ChoreWeight } from "../model/Chore.js";
// STUB — phase 3. Resolves so tests fail on their assertions, not on a missing module.
export function weight(_w: ChoreWeight): number { return 0; }
